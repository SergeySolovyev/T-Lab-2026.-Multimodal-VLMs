defaults:
  - base

grpo:
  init_checkpoint: artifacts/sft/checkpoint_last.pt
  mode: action
  total_updates: 2000
  rollout_episodes_per_update: 8
  group_size: 8
  lr: 5e-6
  clip_eps: 0.2
  kl_beta: 0.02
  max_gen_tokens: 4
  reward:
    success_bonus: 1.0
    step_penalty: 0.01
